import App from "./classes/App";

// Esta é uma instância do servidor. Sinta-se à vontade para mudar a porta passando-a como parâmetro
const app = new App();

// Este comando lança o servidor
app.listen();